addEventListener(submit = "clicked");   // Listen for "submit"
const event.preventDefault();           // Stop browser from reloading
const [Name] = [Name].value;            // New greeting string
const input ${"Hello, [Name]!"};        // Output string
const = getElementById ("Greetings!")reset();   //Reset input